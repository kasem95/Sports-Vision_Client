export default class User{
    constructor(userID,email,password,userName,imageURL){
        this.userID = userID
        this.email = email
        this.password = password
        this.username = userName
        this.imageURL = imageURL
    }

}